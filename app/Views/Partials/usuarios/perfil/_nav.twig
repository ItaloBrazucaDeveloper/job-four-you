{% set url = getCurrentUrl() %}
<div class="border-b border-gray-200">
  <nav class="flex overflow-x-auto">
    <a href="/usuarios/meu-perfil" class="{{ url starts with '/usuarios/meu-perfil' ? 'border-b-2 border-yellow-500 text-yellow-500' : 'border-b-2 border-transparent text-gray-500 hover:text-gray-700' }} px-6 py-4 font-medium whitespace-nowrap">
      <i class="fas fa-user-circle mr-2"></i> Perfil
    </a>
    <a href="/usuarios/meus-favoritos" class="{{ url starts with '/usuarios/meus-favoritos' ? 'border-b-2 border-yellow-500 text-yellow-500' : 'border-b-2 border-gray-500 text-gray-800 opacity-50' }} px-6 py-4 font-medium whitespace-nowrap">
      <i class="fas fa-heart mr-2"></i> Favoritos
    </a>
    {% if session('usuario_autenticado').grupo == 'PRESTADOR' %}
      <a href="/usuarios/meus-servicos" class="{{ url starts with '/usuarios/meus-servicos' ? 'border-b-2 border-yellow-500 text-yellow-500' : 'border-b-2 border-transparent text-gray-500 hover:text-gray-700' }} px-6 py-4 font-medium whitespace-nowrap">
        <i class="fas fa-briefcase mr-2"></i> Meus Serviços
      </a>
    {% endif %}
  </nav>
</div> 